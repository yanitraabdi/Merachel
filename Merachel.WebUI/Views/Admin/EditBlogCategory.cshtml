@model Merachel.Domain.Entities.BlogCategory

@{
    ViewBag.Title = "EditBlogCategory";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Edit Blog Category</h2>

<div>
    @Html.ActionLink("Back to List", "BlogCategory")
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>BlogCategory</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.BlogCategoryID)

        <div class="form-group">
            @Html.LabelFor(model => model.BlogCategoryName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.BlogCategoryName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.BlogCategoryName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}
